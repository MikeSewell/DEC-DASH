---
phase: 15-donation-performance-charts
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/dashboard/DonationPerformance.tsx
  - src/hooks/useQuickBooks.ts
autonomous: true
requirements: [DON-01, DON-02, DON-03, DON-04]

must_haves:
  truths:
    - "DonationPerformance section displays a populated line chart showing 12 months of QB income data when accounts are configured"
    - "The income chart breaks down revenue by QB account category with multiple datasets on the line chart"
    - "When no accounts have been designated, the chart shows a clear 'Configure donation accounts in Admin' prompt with a link to /admin?tab=quickbooks"
    - "When QB is not connected, the chart shows the existing 'Connect QuickBooks' empty state"
  artifacts:
    - path: "src/components/dashboard/DonationPerformance.tsx"
      provides: "Rewritten DonationPerformance component powered by useIncomeTrend hook with multi-line income breakdown chart and unconfigured/empty states"
      min_lines: 120
  key_links:
    - from: "src/components/dashboard/DonationPerformance.tsx"
      to: "src/hooks/useQuickBooks.ts"
      via: "useIncomeTrend hook"
      pattern: "useIncomeTrend"
    - from: "src/components/dashboard/DonationPerformance.tsx"
      to: "src/hooks/useQuickBooks.ts"
      via: "useQuickBooksConfig hook"
      pattern: "useQuickBooksConfig"
---

<objective>
Rewrite DonationPerformance.tsx to use the new useIncomeTrend hook instead of useDonations, rendering a multi-line income chart with account breakdown and proper empty/unconfigured states.

Purpose: Replace the placeholder donation chart with real QB income data visualization.
Output: Updated `DonationPerformance.tsx` component showing live income trends from QB.
</objective>

<execution_context>
@/Users/mastermac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mastermac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-donation-performance-charts/15-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 15-01 -->

From convex/quickbooks.ts (created in 15-01) -- getIncomeTrend return shape:
```typescript
// When no accounts designated:
{ configured: false, months: [], accounts: [], fetchedAt: number }

// When accounts are designated and data exists:
{
  configured: true,
  months: Array<{
    label: string;           // e.g. "Apr 2025", "May 2025" — QB month header
    total: number;           // sum across all designated accounts
    breakdown: Record<string, number>;  // accountName -> amount for this month
  }>,
  accounts: string[],        // distinct account names that have data
  fetchedAt: number,
}
```

From src/hooks/useQuickBooks.ts (created in 15-01):
```typescript
export function useIncomeTrend() {
  return useQuery(api.quickbooks.getIncomeTrend);
}
// Returns undefined (loading) | null (no cache) | { configured, months, accounts, fetchedAt }
```

From src/hooks/useQuickBooks.ts (existing):
```typescript
export function useQuickBooksConfig() {
  return useQuery(api.quickbooks.getConfig);
}
// Returns undefined (loading) | null (not connected) | { _id, realmId, companyName, connectedAt, isExpired }
```

Existing DonationPerformance.tsx imports and registrations to keep:
```typescript
import { Line } from "react-chartjs-2";
import {
  Chart as ChartJS, CategoryScale, LinearScale, PointElement, LineElement,
  Title, Tooltip, Legend, Filler,
} from "chart.js";
import { ChartSkeleton } from "@/components/dashboard/skeletons/ChartSkeleton";
import { formatCurrency, timeAgo } from "@/lib/utils";

ChartJS.register(CategoryScale, LinearScale, PointElement, LineElement, Title, Tooltip, Legend, Filler);
```

Design system chart palette:
```typescript
const PALETTE = ["#1B5E6B", "#2B9E9E", "#5BBFB5", "#6BBF59", "#8CC63F", "#7DD4C8", "#1A7A7A", "#2D6A4F"];
```

Chart tooltip style:
```typescript
backgroundColor: "rgba(27,67,50,0.9)", cornerRadius: 12, padding: 12,
titleFont: { family: "'Nunito', sans-serif" }, bodyFont: { family: "'Nunito', sans-serif" }
```

Card/stat design pattern:
```tsx
<div className="rounded-2xl border border-border bg-surface p-5 text-center shadow-[var(--warm-shadow-sm)] hover-lift">
  <p className="text-2xl font-bold text-foreground">{value}</p>
  <p className="text-xs text-muted mt-1">{label}</p>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DonationPerformance.tsx with useIncomeTrend, multi-line chart, and proper empty states</name>
  <files>src/components/dashboard/DonationPerformance.tsx</files>
  <action>
Completely rewrite `src/components/dashboard/DonationPerformance.tsx`. Keep the `"use client"` directive and Chart.js registrations. Replace ALL the existing component logic.

**Imports:**
```typescript
"use client";

import { Line } from "react-chartjs-2";
import {
  Chart as ChartJS,
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend,
  Filler,
} from "chart.js";
import { useIncomeTrend, useQuickBooksConfig } from "@/hooks/useQuickBooks";
import { ChartSkeleton } from "@/components/dashboard/skeletons/ChartSkeleton";
import { formatCurrency, timeAgo } from "@/lib/utils";

ChartJS.register(
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend,
  Filler
);
```

**Constants:**
```typescript
const PALETTE = [
  "#1B5E6B", "#2B9E9E", "#5BBFB5", "#6BBF59",
  "#8CC63F", "#7DD4C8", "#1A7A7A", "#2D6A4F",
];
```

**Component logic — `export default function DonationPerformance()`:**

1. Call hooks:
```typescript
const config = useQuickBooksConfig();
const incomeTrend = useIncomeTrend();
```

2. **Loading state:** If `config === undefined || incomeTrend === undefined`, return `<ChartSkeleton />`.

3. **QB not connected state:** If `config === null`, return a centered empty state:
```tsx
<div className="flex flex-col items-center justify-center py-12 text-muted">
  <svg className="h-10 w-10 mb-3 opacity-40" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
    <path strokeLinecap="round" strokeLinejoin="round" d="M2.25 18.75a60.07 60.07 0 0115.797 2.101c.727.198 1.453-.342 1.453-1.096V18.75M3.75 4.5v.75A.75.75 0 013 6h-.75m0 0v-.375c0-.621.504-1.125 1.125-1.125H20.25M2.25 6v9m18-10.5v.75c0 .414.336.75.75.75h.75m-1.5-1.5h.375c.621 0 1.125.504 1.125 1.125v9.75c0 .621-.504 1.125-1.125 1.125h-.375m1.5-1.5H21a.75.75 0 00-.75.75v.75m0 0H3.75m0 0h-.375a1.125 1.125 0 01-1.125-1.125V15m1.5 1.5v-.75A.75.75 0 003 15h-.75M15 10.5a3 3 0 11-6 0 3 3 0 016 0zm3 0h.008v.008H18V10.5zm-12 0h.008v.008H6V10.5z" />
  </svg>
  <p className="text-sm">No donation data available yet.</p>
  <p className="text-xs mt-1">Connect QuickBooks to import income information.</p>
  <a href="/admin?tab=quickbooks" className="text-primary hover:underline text-xs mt-2 inline-block">
    Connect QuickBooks &rarr;
  </a>
</div>
```

4. **Not configured state (DON-04):** If `incomeTrend === null || !incomeTrend.configured`, return:
```tsx
<div className="flex flex-col items-center justify-center py-12 text-muted">
  <svg className="h-10 w-10 mb-3 opacity-40" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
    <path strokeLinecap="round" strokeLinejoin="round" d="M10.343 3.94c.09-.542.56-.94 1.11-.94h1.093c.55 0 1.02.398 1.11.94l.149.894c.07.424.384.764.78.93.398.164.855.142 1.205-.108l.737-.527a1.125 1.125 0 011.45.12l.773.774c.39.389.44 1.002.12 1.45l-.527.737c-.25.35-.272.806-.107 1.204.165.397.505.71.93.78l.893.15c.543.09.94.56.94 1.109v1.094c0 .55-.397 1.02-.94 1.11l-.893.149c-.425.07-.765.383-.93.78-.165.398-.143.854.107 1.204l.527.738c.32.447.269 1.06-.12 1.45l-.774.773a1.125 1.125 0 01-1.449.12l-.738-.527c-.35-.25-.806-.272-1.203-.107-.397.165-.71.505-.781.929l-.149.894c-.09.542-.56.94-1.11.94h-1.094c-.55 0-1.019-.398-1.11-.94l-.148-.894c-.071-.424-.384-.764-.781-.93-.398-.164-.854-.142-1.204.108l-.738.527c-.447.32-1.06.269-1.45-.12l-.773-.774a1.125 1.125 0 01-.12-1.45l.527-.737c.25-.35.273-.806.108-1.204-.165-.397-.505-.71-.93-.78l-.894-.15c-.542-.09-.94-.56-.94-1.109v-1.094c0-.55.398-1.02.94-1.11l.894-.149c.424-.07.765-.383.93-.78.165-.398.143-.854-.107-1.204l-.527-.738a1.125 1.125 0 01.12-1.45l.773-.773a1.125 1.125 0 011.45-.12l.737.527c.35.25.807.272 1.204.107.397-.165.71-.505.78-.929l.15-.894z" />
    <path strokeLinecap="round" strokeLinejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
  </svg>
  <p className="text-sm font-medium">Configure donation accounts in Admin</p>
  <p className="text-xs mt-1">Select which QB income accounts to display in this chart.</p>
  <a href="/admin?tab=quickbooks" className="text-primary hover:underline text-xs mt-2 inline-block">
    Open Admin Settings &rarr;
  </a>
</div>
```

5. **Data present — compute summary stats:**
```typescript
const { months, accounts, fetchedAt } = incomeTrend;

// Grand total across all months
const grandTotal = months.reduce((sum, m) => sum + m.total, 0);

// Average monthly income
const avgMonthly = months.length > 0 ? grandTotal / months.length : 0;

// Current month vs previous month trend
const currentMonthTotal = months.length > 0 ? months[months.length - 1].total : 0;
const prevMonthTotal = months.length > 1 ? months[months.length - 2].total : 0;
const trendPct = prevMonthTotal > 0
  ? Math.round(((currentMonthTotal - prevMonthTotal) / prevMonthTotal) * 100)
  : currentMonthTotal > 0 ? 100 : 0;
const trendPositive = currentMonthTotal >= prevMonthTotal;
```

6. **Build chart data (DON-01 + DON-02):**

If there is only 1 account, show a single filled line (like the original). If there are multiple accounts, show one line per account plus a total line. This breakdown is the DON-02 requirement.

```typescript
const labels = months.map((m) => m.label);

const datasets = [];

// If multiple accounts, add a line per account
if (accounts.length > 1) {
  for (let i = 0; i < accounts.length; i++) {
    const accountName = accounts[i];
    const color = PALETTE[i % PALETTE.length];
    datasets.push({
      label: accountName,
      data: months.map((m) => m.breakdown[accountName] ?? 0),
      borderColor: color,
      backgroundColor: "transparent",
      tension: 0.3,
      pointRadius: 3,
      pointHoverRadius: 5,
      pointBackgroundColor: color,
      pointBorderColor: "rgba(255,254,249,0.9)",
      pointBorderWidth: 1.5,
      borderWidth: 2,
    });
  }
}

// Total line (always shown)
datasets.push({
  label: accounts.length > 1 ? "Total Income" : (accounts[0] ?? "Income"),
  data: months.map((m) => m.total),
  borderColor: "#2D6A4F",
  backgroundColor: accounts.length > 1 ? "transparent" : "rgba(45, 106, 79, 0.1)",
  fill: accounts.length <= 1,
  tension: 0.3,
  pointRadius: 4,
  pointHoverRadius: 6,
  pointBackgroundColor: "#1B4332",
  pointBorderColor: "#FFFEF9",
  pointBorderWidth: 2,
  borderWidth: accounts.length > 1 ? 3 : 2,
});

const chartData = { labels, datasets };
```

7. **Chart options:**
```typescript
const chartOptions = {
  responsive: true,
  maintainAspectRatio: false,
  interaction: {
    mode: "index" as const,
    intersect: false,
  },
  plugins: {
    legend: {
      display: accounts.length > 1,
      position: "top" as const,
      labels: {
        usePointStyle: true,
        pointStyle: "circle",
        padding: 16,
        font: { size: 11, family: "'Nunito', sans-serif" },
      },
    },
    tooltip: {
      backgroundColor: "rgba(27,67,50,0.9)",
      cornerRadius: 12,
      padding: 12,
      titleFont: { family: "'Nunito', sans-serif" },
      bodyFont: { family: "'Nunito', sans-serif" },
      callbacks: {
        label: (ctx: { dataset: { label?: string }; raw: unknown }) =>
          `${ctx.dataset.label}: ${formatCurrency(ctx.raw as number)}`,
      },
    },
  },
  scales: {
    y: {
      beginAtZero: true,
      ticks: {
        callback: (value: string | number) => formatCurrency(Number(value)),
        font: { family: "'Nunito', sans-serif", size: 11 },
      },
      grid: {
        color: "rgba(45,106,79,0.06)",
      },
    },
    x: {
      grid: { display: false },
      ticks: {
        font: { family: "'Nunito', sans-serif", size: 11 },
      },
    },
  },
};
```

8. **Render JSX:**
```tsx
return (
  <div className="space-y-6">
    {/* Stat cards */}
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
      <div className="rounded-2xl border border-border bg-surface p-5 text-center shadow-[var(--warm-shadow-sm)] hover-lift">
        <p className="text-2xl font-bold text-foreground">{formatCurrency(grandTotal)}</p>
        <p className="text-xs text-muted mt-1">Total Income (12 mo)</p>
      </div>
      <div className="rounded-2xl border border-border bg-surface p-5 text-center shadow-[var(--warm-shadow-sm)] hover-lift">
        <p className="text-2xl font-bold text-foreground">{formatCurrency(avgMonthly)}</p>
        <p className="text-xs text-muted mt-1">Avg Monthly</p>
      </div>
      <div className="rounded-2xl border border-border bg-surface p-5 text-center shadow-[var(--warm-shadow-sm)] hover-lift">
        <p className={`text-2xl font-bold ${trendPositive ? "text-success" : "text-danger"}`}>
          {trendPositive ? "+" : ""}{trendPct}%
        </p>
        <p className="text-xs text-muted mt-1">vs Previous Month</p>
      </div>
    </div>

    {/* Line chart */}
    {months.length > 0 ? (
      <div style={{ height: 300 }}>
        <Line data={chartData} options={chartOptions} />
      </div>
    ) : (
      <p className="text-sm text-muted text-center py-8">
        No monthly income data to chart.
      </p>
    )}

    {/* Sync timestamp */}
    {fetchedAt && (
      <p className="text-xs text-muted text-right">
        Last synced: {timeAgo(fetchedAt)}
      </p>
    )}
  </div>
);
```

The complete file should be roughly 180-220 lines.
  </action>
  <verify>
    <automated>cd "/Users/mastermac/Desktop/DEC-DASH 2.0" && wc -l src/components/dashboard/DonationPerformance.tsx && grep -c "useIncomeTrend\|useQuickBooksConfig\|Configure donation accounts\|admin?tab=quickbooks" src/components/dashboard/DonationPerformance.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DonationPerformance.tsx is 120+ lines. Uses useIncomeTrend and useQuickBooksConfig hooks. Contains "Configure donation accounts in Admin" prompt for unconfigured state. Links to /admin?tab=quickbooks. Multi-line chart renders when accounts are configured. npm run build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without TypeScript errors
2. `src/components/dashboard/DonationPerformance.tsx` is 120+ lines and uses `useIncomeTrend` hook
3. When QB not connected: shows "Connect QuickBooks" empty state with link to /admin?tab=quickbooks
4. When QB connected but no accounts designated: shows "Configure donation accounts in Admin" prompt with link to /admin?tab=quickbooks (DON-04)
5. When accounts designated and data exists: renders stat cards (total income, avg monthly, trend %) and a multi-line chart with one line per account plus total line (DON-01, DON-02)
6. Chart legend is shown when multiple accounts are present, hidden when single account
7. Chart tooltip shows formatted currency per dataset
8. "Last synced" timestamp shown at bottom
</verification>

<success_criteria>
- Dashboard DonationPerformance section displays a populated line chart showing 12 months of QB income data (DON-01)
- The income chart breaks down revenue by QB account category with multiple datasets/lines so Kareem can see which sources contribute each month (DON-02)
- The chart reflects admin-designated accounts on render — changing designation in admin and returning to dashboard shows updated chart (DON-03 integration)
- When no accounts have been designated, the DonationPerformance section shows a clear "Configure donation accounts in Admin" prompt rather than an empty or broken chart (DON-04)
- Chart styling matches the warm-green design system (PALETTE, Nunito font, warm grid lines, rounded tooltips)
- Stat cards show total income, average monthly, and month-over-month trend
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-donation-performance-charts/15-02-SUMMARY.md`
</output>
