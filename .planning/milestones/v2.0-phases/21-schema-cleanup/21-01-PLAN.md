---
phase: 21-schema-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/googleSheetsInternal.ts
  - convex/googleSheets.ts
  - convex/schema.ts
  - convex/alertConfig.ts
  - convex/alerts.ts
  - src/hooks/useGrantTracker.ts
  - src/components/dashboard/ProgramsLegal.tsx
  - src/components/dashboard/ProgramsCoparent.tsx
  - src/components/admin/AlertsConfig.tsx
autonomous: false
requirements:
  - INFR-02

must_haves:
  truths:
    - "The programDataCache table has zero documents and is removed from the Convex schema"
    - "No backend or frontend code references programDataCache, getProgramDemographics, or upsertProgramParticipant"
    - "The sheetsStalenessHours field is removed from alertConfig schema, backend code, and admin UI"
    - "Dashboard ProgramsLegal and ProgramsCoparent sections render data from the clients table (via getAllDemographics) instead of programDataCache"
    - "npx convex dev --once deploys without schema validation errors"
  artifacts:
    - path: "convex/schema.ts"
      provides: "Schema without programDataCache table and without sheetsStalenessHours in alertConfig"
      contains: "alertConfig: defineTable"
    - path: "convex/googleSheetsInternal.ts"
      provides: "clearProgramDataCache internal mutation, upsertProgramParticipant deleted"
      exports: ["clearProgramDataCache"]
    - path: "src/components/dashboard/ProgramsLegal.tsx"
      provides: "Legal program demographics from Convex clients table via getAllDemographics"
    - path: "src/components/dashboard/ProgramsCoparent.tsx"
      provides: "Co-parent program demographics from Convex clients table via getAllDemographics"
  key_links:
    - from: "convex/googleSheetsInternal.ts"
      to: "programDataCache table"
      via: "clearProgramDataCache mutation deletes all documents"
      pattern: "ctx\\.db\\.query\\(\"programDataCache\"\\)"
    - from: "src/components/dashboard/ProgramsLegal.tsx"
      to: "convex/analytics.ts getAllDemographics"
      via: "useQuery(api.analytics.getAllDemographics)"
      pattern: "api\\.analytics\\.getAllDemographics"
---

<objective>
Clear all programDataCache documents, delete dead code that wrote/read programDataCache (upsertProgramParticipant, getProgramDemographics, useProgramDemographics), rewrite dashboard components that consumed this data to use getAllDemographics from the clients table instead, remove the programDataCache table definition from schema.ts, and remove the sheetsStalenessHours field from alertConfig.

Purpose: Completes INFR-02 by removing the deprecated Sheets-synced programDataCache table and all code that touched it. Also removes the sheetsStalenessHours artifact left over from Phase 20 since it co-locates with the same schema.ts edit.

Output: Schema deployed without programDataCache table, all dead code deleted, dashboard components rewired.
</objective>

<execution_context>
@/Users/mastermac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mastermac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-schema-cleanup/21-RESEARCH.md

@convex/schema.ts
@convex/googleSheetsInternal.ts
@convex/googleSheets.ts
@convex/alertConfig.ts
@convex/alerts.ts
@src/hooks/useGrantTracker.ts
@src/components/dashboard/ProgramsLegal.tsx
@src/components/dashboard/ProgramsCoparent.tsx
@src/components/admin/AlertsConfig.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From convex/analytics.ts — the query that replaces getProgramDemographics:
```typescript
export const getAllDemographics = query({
  args: { programId: v.optional(v.id("programs")) },
  handler: async (ctx, { programId }) => {
    // Returns: { total, active, completed, genderDistribution, ethnicityDistribution,
    //            ageDistribution, referralSource, zipDistribution }
    // Each distribution is Array<{ name: string; count: number }>
  },
});
```

From convex/programs.ts — used to look up program IDs by type:
```typescript
export const list = query({
  args: {},
  handler: async (ctx) => {
    return await ctx.db.query("programs").collect();
    // Returns: Array<{ _id, name, type, description, isActive, createdAt }>
  },
});
```

From src/hooks/useGrantTracker.ts — exports to modify:
```typescript
export function useProgramDemographics(programType: string) { ... }  // DELETE
export function useSheetsConfig() { ... }                             // KEEP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear programDataCache, delete dead backend code, remove sheetsStalenessHours</name>
  <files>convex/googleSheetsInternal.ts, convex/googleSheets.ts, convex/schema.ts, convex/alertConfig.ts, convex/alerts.ts, src/hooks/useGrantTracker.ts, src/components/admin/AlertsConfig.tsx</files>
  <action>
**Step 1: Add clearProgramDataCache internalMutation to googleSheetsInternal.ts.**

Add this function at the bottom of `convex/googleSheetsInternal.ts`:

```typescript
// Phase 21: Clear all programDataCache documents before table removal
export const clearProgramDataCache = internalMutation({
  args: {},
  handler: async (ctx) => {
    const docs = await ctx.db.query("programDataCache").collect();
    for (const doc of docs) {
      await ctx.db.delete(doc._id);
    }
    return { deleted: docs.length };
  },
});
```

**Step 2: Delete `upsertProgramParticipant` from googleSheetsInternal.ts.**

Remove the entire `upsertProgramParticipant` internalMutation function (lines 58-88 — the function that writes to programDataCache). This is dead code since Phase 20 deleted syncProgramData.

**Step 3: Delete `getProgramDemographics` from googleSheets.ts.**

Remove the entire `getProgramDemographics` query function (lines 85-136 — the function that reads from programDataCache). No callers remain after the dashboard components are rewritten in Task 2.

**Step 4: Remove `useProgramDemographics` from useGrantTracker.ts.**

Delete the `useProgramDemographics` function (lines 22-24) and remove the `api.googleSheets.getProgramDemographics` import it depends on. Keep all other exports (`useGrants`, `useActiveGrants`, `useGrantDeadlines`, `useSheetsConfig`, `useSheetsSync`).

**Step 5: Remove sheetsStalenessHours from alertConfig backend.**

In `convex/alertConfig.ts`:
- Remove `sheetsStalenessHours: 2` from `ALERT_DEFAULTS` (line 10)
- Remove `sheetsStalenessHours: config.sheetsStalenessHours` from the `get` query return object (line 26)
- Remove `sheetsStalenessHours: v.number()` from the `update` mutation args (line 41)

In `convex/alerts.ts`:
- Remove `sheetsStalenessHours: configRow?.sheetsStalenessHours ?? 2` from the config object (line 24). The remaining config object should have: deadlineWindowDays, budgetVariancePct, qbStalenessHours, calendarStalenessHours.

**Step 6: Remove sheetsStalenessHours from AlertsConfig.tsx UI.**

In `src/components/admin/AlertsConfig.tsx`:
- Remove `sheetsStalenessHours: 2` from the DEFAULTS object
- Remove the `sheetsStalenessHours` state variable and its `useState` call
- Remove the `setSheetsStalenessHours(config.sheetsStalenessHours)` in the useEffect
- Remove the `setSheetsStalenessHours(DEFAULTS.sheetsStalenessHours)` in the reset handler
- Remove `sheetsStalenessHours` from the `handleSave` call args
- Remove the "Google Sheets Staleness" input field from the JSX

**Step 7: Remove sheetsStalenessHours and programDataCache from schema.ts.**

In `convex/schema.ts`:
- Delete the entire `programDataCache` table definition (lines 115-132, from `programDataCache: defineTable({` through the closing `.index("by_programType", ["programType"]),`)
- Remove `sheetsStalenessHours: v.number(),` from the `alertConfig` table definition (line 422)

**IMPORTANT:** Do NOT deploy schema yet. Task 2 must update dashboard components first, then the checkpoint will instruct the user to run clearProgramDataCache and deploy.
  </action>
  <verify>
    <automated>cd "/Users/mastermac/Desktop/DEC-DASH 2.0" && grep -c "programDataCache" convex/schema.ts && echo "FAIL: programDataCache still in schema" || echo "PASS: programDataCache removed from schema"; grep -c "upsertProgramParticipant" convex/googleSheetsInternal.ts && echo "FAIL: upsertProgramParticipant still exists" || echo "PASS: upsertProgramParticipant removed"; grep -c "getProgramDemographics" convex/googleSheets.ts && echo "FAIL: getProgramDemographics still exists" || echo "PASS: getProgramDemographics removed"; grep -c "useProgramDemographics" src/hooks/useGrantTracker.ts && echo "FAIL: useProgramDemographics still exported" || echo "PASS: useProgramDemographics removed"; grep -c "sheetsStalenessHours" convex/alertConfig.ts && echo "FAIL: sheetsStalenessHours still in alertConfig" || echo "PASS: sheetsStalenessHours removed from alertConfig"; grep -c "sheetsStalenessHours" convex/schema.ts && echo "FAIL: sheetsStalenessHours still in schema" || echo "PASS: sheetsStalenessHours removed from schema"</automated>
  </verify>
  <done>
    - clearProgramDataCache internalMutation added to googleSheetsInternal.ts
    - upsertProgramParticipant deleted from googleSheetsInternal.ts
    - getProgramDemographics deleted from googleSheets.ts
    - useProgramDemographics deleted from useGrantTracker.ts
    - sheetsStalenessHours removed from alertConfig.ts (ALERT_DEFAULTS, get query, update mutation)
    - sheetsStalenessHours removed from alerts.ts config object
    - sheetsStalenessHours removed from AlertsConfig.tsx (state, useEffect, reset, save args, UI input)
    - programDataCache table definition removed from schema.ts
    - sheetsStalenessHours removed from alertConfig table in schema.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ProgramsLegal and ProgramsCoparent to use getAllDemographics</name>
  <files>src/components/dashboard/ProgramsLegal.tsx, src/components/dashboard/ProgramsCoparent.tsx</files>
  <action>
Both `ProgramsLegal.tsx` and `ProgramsCoparent.tsx` currently call `useProgramDemographics("legal"|"coparent")` which reads from the now-deleted `programDataCache` table. Rewrite both to use `getAllDemographics` from `convex/analytics.ts` instead.

**ProgramsLegal.tsx changes:**

1. Replace imports:
   - Remove: `import { useProgramDemographics, useSheetsConfig } from "@/hooks/useGrantTracker";`
   - Add: `import { useQuery } from "convex/react";`
   - Add: `import { api } from "../../../../convex/_generated/api";`

2. Replace data fetching inside the component:
   - Remove: `const demographics = useProgramDemographics("legal");`
   - Remove: `const sheetsConfig = useSheetsConfig();`
   - Add: `const programs = useQuery(api.programs.list);`
   - Add: Find the legal program ID: `const legalProgram = programs?.find((p) => p.type === "legal");`
   - Add: `const demographics = useQuery(api.analytics.getAllDemographics, legalProgram ? { programId: legalProgram._id } : "skip");`

3. Replace the loading/empty states:
   - Remove the `sheetsConfig === null` guard (no longer dependent on Sheets config)
   - Update the loading check: `if (demographics === undefined || programs === undefined)` returns ChartSkeleton
   - Keep the `demographics.total === 0` empty state check but update the text to "No legal program data yet." (remove "synced yet" and sync cycle reference)

4. The return shape of `getAllDemographics` differs from `getProgramDemographics`:
   - `getAllDemographics` returns: `{ total, active, completed, genderDistribution, ethnicityDistribution, ageDistribution, referralSource, zipDistribution }`
   - `getProgramDemographics` returned: `{ total, active, completed, avgSessions, genderDistribution, ethnicityDistribution, ageDistribution, outcomeDistribution, reasonForVisit, referralSource }`
   - Changes needed in destructuring:
     - Remove `avgSessions` (not available; replace stat card with zip code count or remove the card)
     - Remove `outcomeDistribution` (not available; remove the "Program Outcomes" chart section)
     - Remove `reasonForVisit` (not available from getAllDemographics; remove the "Reason for Visit" chart section)
     - Add `zipDistribution` (new)
   - For the stat row, replace the "Avg Sessions" card with a "Zip Codes" card showing `zipDistribution.length` (number of unique zip codes).
   - Replace the "Reason for Visit" chart with a "Zip Code Distribution" chart using the same horizontal bar format with `zipDistribution`.
   - Remove the "Program Outcomes" pie chart section entirely (outcomeDistribution not available).

**ProgramsCoparent.tsx changes:**

Apply the exact same pattern as ProgramsLegal but for co-parent:
- `const coparentProgram = programs?.find((p) => p.type === "coparent");`
- `const demographics = useQuery(api.analytics.getAllDemographics, coparentProgram ? { programId: coparentProgram._id } : "skip");`
- Same destructuring changes (remove avgSessions/outcomeDistribution, add zipDistribution)
- Replace "Avg Sessions" stat card with "Zip Codes" card
- Remove "Program Outcomes" chart section
- Add "Zip Code Distribution" horizontal bar chart

**IMPORTANT:** The `useSheetsConfig` import is still used by other consumers. Do NOT remove it from `useGrantTracker.ts` — only the `useProgramDemographics` export was removed in Task 1.
  </action>
  <verify>
    <automated>cd "/Users/mastermac/Desktop/DEC-DASH 2.0" && grep -c "useProgramDemographics" src/components/dashboard/ProgramsLegal.tsx && echo "FAIL: ProgramsLegal still uses useProgramDemographics" || echo "PASS: ProgramsLegal rewritten"; grep -c "useProgramDemographics" src/components/dashboard/ProgramsCoparent.tsx && echo "FAIL: ProgramsCoparent still uses useProgramDemographics" || echo "PASS: ProgramsCoparent rewritten"; grep -c "getAllDemographics" src/components/dashboard/ProgramsLegal.tsx && echo "PASS: ProgramsLegal uses getAllDemographics" || echo "FAIL: ProgramsLegal missing getAllDemographics"; grep -c "getAllDemographics" src/components/dashboard/ProgramsCoparent.tsx && echo "PASS: ProgramsCoparent uses getAllDemographics" || echo "FAIL: ProgramsCoparent missing getAllDemographics"</automated>
  </verify>
  <done>
    - ProgramsLegal.tsx uses api.analytics.getAllDemographics with programId filter for legal program
    - ProgramsCoparent.tsx uses api.analytics.getAllDemographics with programId filter for coparent program
    - No references to useProgramDemographics or programDataCache remain anywhere in frontend code
    - Both components handle loading and empty states correctly
    - avgSessions and outcomeDistribution removed (not in getAllDemographics return shape)
    - zipDistribution chart added in place of removed sections
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify programDataCache removal and dashboard components</name>
  <files>convex/schema.ts</files>
  <action>
    Human verification checkpoint. The following was built:
    - programDataCache dead code deleted from backend and frontend
    - Dashboard components (ProgramsLegal, ProgramsCoparent) rewired to use getAllDemographics from the clients table
    - sheetsStalenessHours removed from alertConfig schema, backend code, and admin UI
    - programDataCache table definition removed from schema.ts

    Steps for the user:
    1. Run `npx convex run googleSheetsInternal:clearProgramDataCache` — verify it returns `{ deleted: N }` (may be 0 if table was already empty)
    2. Run `npx convex dev --once` to deploy the schema — verify it completes without errors
    3. Open the dashboard — verify "Legal Aid Program" and "Co-Parent Counseling" sections render demographics charts from clients table data (not "Connect Google Sheets" placeholder)
    4. Open Admin > Alerts Configuration — verify there is NO "Google Sheets Staleness" input field
    5. Open the Convex dashboard and verify `programDataCache` table no longer appears
  </action>
  <verify>
    <automated>cd "/Users/mastermac/Desktop/DEC-DASH 2.0" && grep -rc "programDataCache" convex/ src/ | grep -v ":0$" | grep -v "clearProgramDataCache" && echo "FAIL: programDataCache references remain" || echo "PASS: programDataCache fully removed"</automated>
  </verify>
  <done>User confirms dashboard renders correctly, schema deploys, programDataCache table gone from Convex dashboard</done>
</task>

</tasks>

<verification>
1. `grep -r "programDataCache" convex/ src/` returns zero results (table and all code references removed)
2. `grep -r "upsertProgramParticipant" convex/ src/` returns zero results
3. `grep -r "getProgramDemographics" convex/ src/` returns zero results
4. `grep -r "useProgramDemographics" src/` returns zero results
5. `grep -r "sheetsStalenessHours" convex/ src/` returns zero results
6. `npx convex dev --once` deploys successfully with no schema errors
7. Dashboard ProgramsLegal and ProgramsCoparent sections display data from clients table
</verification>

<success_criteria>
- programDataCache table has zero documents and is removed from Convex schema
- All dead code (upsertProgramParticipant, getProgramDemographics, useProgramDemographics) is deleted
- sheetsStalenessHours is removed from schema, backend code, and admin UI
- Dashboard components use getAllDemographics query instead of programDataCache
- Schema deploys cleanly via npx convex dev --once
</success_criteria>

<output>
After completion, create `.planning/phases/21-schema-cleanup/21-01-SUMMARY.md`
</output>
